@{
    // Every view under Views/ uses _Layout.cshtml unless it explicitly
    // overrides Layout itself. Without this file, Index.cshtml renders as a
    // bare content fragment — no nav, no footer, no <head>, no CSS link —
    // which looks exactly like "plain, no colors, just text".
    Layout = "_Layout";
}
